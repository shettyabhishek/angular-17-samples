import { AlphaNumDirective } from './alpha-num.directive';

describe('AlphaNumDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaNumDirective();
    expect(directive).toBeTruthy();
  });
});
